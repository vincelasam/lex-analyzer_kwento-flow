// contain test frameworks for testing functions and logics in the lexer 

/* test('keywords', () => {
    tokens = lexer.tokenize('scene');
    assert(tokens[0].type == KEYWORD);
});

test('numbers', () => {
    tokens = lexer.tokenize('42');
    assert(tokens[0].type == NUMBER);
});

test('invalid input', () => {
    tokens = lexer.tokenize('@@@');
    assert(tokens[0].type == ERROR);  // Should return error
}); 

*/