{
  "name": "backend",
  "version": "1.0.0",
  "description": "Hand-coded lexical analyzer for KwentoFlow language",
  "main": "dist/server/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server/server.js",
    "dev": "nodemon --watch src --exec ts-node src/server/server.ts",
    "test": "ts-node src/tests/lexer.test.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["lexer", "compiler", "kwentoflow"],
  "author": "Group 6",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "nodemon": "^3.0.0"
  }
}